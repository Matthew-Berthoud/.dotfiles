name: ndaa
attach: false
root: ~/repos/odam-ndaa/
startup_window: terminal
pre_window:
  - source .venv/bin/activate
  - uv sync
windows:
- editor: nvim
- terminal: git status && git branch
- server: make run-ui
